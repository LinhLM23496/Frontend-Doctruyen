{
  "extends": "@tsconfig/react-native/tsconfig.json",
  "compilerOptions": {
    "types": ["react-native"],
    "baseUrl": "src",
    "paths": {
      "assets": ["assets"],
      "icons": ["asset/icons"],
      "images": ["asset/images"],
      "api": ["api"],
      "components": ["components"],
      "themes": ["themes"],
      "navigation": ["navigation"],
      "query": ["query"],
      "hooks": ["hooks"],
      "lib": ["lib"],
      "stores": ["stores"]
    }
  }
}
