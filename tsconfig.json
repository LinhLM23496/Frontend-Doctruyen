{
  "extends": "@tsconfig/react-native/tsconfig.json",
  "compilerOptions": {
    "types": ["react-native"],
    "baseUrl": "src",
    "paths": {
      "assets": ["assets"],
      "icon": ["asset/icon"],
      "api": ["api"],
      "components": ["components"],
      "themes": ["themes"],
      "navigation": ["navigation"],
      "query": ["query"],
      "hooks": ["hooks"],
      "lib": ["lib"],
      "stores": ["stores"]
    }
  }
}
